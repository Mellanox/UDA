
include makefile.mk  #this file should be generated by uda/rpm/buildall.sh

all:
	rm -rf out
	mkdir -p out
	CLASSPATH=$(CPATH) javac -d out com/mellanox/hadoop/mapred/*.java
	jar -cvf uda.jar -C out com
	rm -rf out

clean:
	rm -rf uda.jar out
